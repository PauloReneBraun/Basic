const d1 = {
    dia: 9,
    mes: 10,
    ano: 2010,
    exibir: function() {
        return`${this.dia}/${this.mes}/${this.ano}`;
    }
};

const d2 = {
    dia: 21,
    mes: 1,
    ano: 2011,
    exibir: function() {
        return`${this.dia}/${this.mes}/${this.ano}`;
    }
};

const d3 = {
    dia: 10,
    mes: 8,
    ano: 2023,
    exibir: function() {
        return`${this.dia}/${this.mes}/${this.ano}`;
    }
};