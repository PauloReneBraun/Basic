const numeros = [1, 2, 626, 4, 5, 6, 15, 51];

for(let i = 0; 1 < numeros.length; i++ ) {
    console.log(numeros[i]);
}

for(let i = 0; 1 < numeros.length; i+= 2 ) {
    console.log(numeros[i]);
}